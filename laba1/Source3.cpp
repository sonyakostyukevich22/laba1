#include <math.h>
#include <iostream>
using namespace std;

//3. sinx + y + cos y

int main()
{
	int x, y;
	x = 1;
	y = 1;
	sin(x);
	cos(y);

	cout << (sin(x)) + y + (cos(y)) << " ";

	system("pause");
	return 0;
}